<h1>My Brews</h1>

	<li>
		<%= link_to "Create new brew", new_brew_path %>
	</li>

<ul id="brews">
	<% @brews.each do |brew| %>
		<li>
			<%= link_to brew.name, brew_path(brew) %>
		</li>
	<% end %>
</ul>

<%= link_to "New Brew!", new_brew_path %>